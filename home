<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Artist Portfolio — Music & Film Photography</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <!-- Titles: Playfair Display, Body: Inter, Accent: Caveat (handwritten) -->
  <link href="https://fonts.googleapis.com/css2?family=Caveat:wght@400;600&family=Inter:wght@300;400;500;700&family=Playfair+Display:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #f3efe7;         /* paper beige */
      --ink: #1e1c1a;        /* warm near-black */
      --muted: #6b6a66;      /* muted gray */
      --accent: #7a8b7a;     /* muted green */
      --link: #5c6f92;       /* slate blue */
      --photo-edge: rgba(0,0,0,0.08);
    }

    /* Section-tinted backgrounds (set via body classes) */
    body.at-hero { --bg: #f3efe7; }
    body.at-music { --bg: #f6f3ec; }
    body.at-photos { --bg: #f2f4f1; }
    body.at-about { --bg: #f4f1f6; }

    * { box-sizing: border-box; }
    html { scroll-behavior: smooth; }
    body {
      margin: 0;
      font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      color: var(--ink);
      background: var(--bg);
      transition: background-color 500ms ease;
      line-height: 1.6;
    }

    /* Subtle paper texture using layered gradients */
    body::before {
      content: "";
      position: fixed; inset: 0; pointer-events: none; z-index: 0;
      background:
        radial-gradient(100px 100px at 10% 10%, rgba(0,0,0,0.03), transparent 70%),
        radial-gradient(120px 80px at 80% 20%, rgba(0,0,0,0.02), transparent 70%),
        radial-gradient(80px 120px at 30% 90%, rgba(0,0,0,0.02), transparent 70%);
      mix-blend-mode: multiply;
    }

    .container { width: min(1100px, 92vw); margin: 0 auto; position: relative; z-index: 1; }

    /* NAV */
    .nav {
      position: fixed; top: 18px; right: 22px; z-index: 10;
      display: flex; gap: 18px; align-items: center;
      font-weight: 500; letter-spacing: 0.2px;
      background: rgba(255,255,255,0.4); backdrop-filter: blur(6px);
      border: 1px solid rgba(0,0,0,0.06); border-radius: 999px; padding: 8px 14px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.08);
    }
    .nav a {
      text-decoration: none; color: var(--ink); opacity: 0.85;
      padding: 6px 10px; border-radius: 999px; transition: 180ms ease;
    }
    .nav a:hover { background: rgba(0,0,0,0.06); opacity: 1; }

    /* HERO */
    .hero {
      min-height: 92vh; display: grid; place-items: center; position: relative;
      padding: 10vh 0 8vh;
      background: linear-gradient(rgba(40,39,35,0.22), rgba(40,39,35,0.22)),
                  url('https://images.unsplash.com/photo-1519681393784-d120267933ba?q=80&w=1920&auto=format&fit=crop');
      background-size: cover; background-position: center;
      color: #fff; text-align: center;
    }
    .hero .title { font-family: 'Playfair Display', serif; font-size: clamp(38px, 6vw, 86px); line-height: 1.05; margin: 0 0 10px; }
    .hero .subtitle { font-size: clamp(14px, 2vw, 18px); opacity: 0.95; letter-spacing: 1px; text-transform: lowercase; }
    .hero .hand {
      font-family: 'Caveat', cursive; font-size: clamp(18px, 3.2vw, 28px); margin-top: 14px; opacity: 0.95;
    }

    .scrollhint { position: absolute; bottom: 22px; left: 50%; transform: translateX(-50%); font-size: 13px; opacity: 0.85; }

    /* SECTION SHELL */
    section { padding: 96px 0; }
    section .section-title {
      font-family: 'Playfair Display', serif; font-weight: 600; font-size: clamp(28px, 3.6vw, 46px);
      margin: 0 0 22px; letter-spacing: 0.2px;
    }
    .section-note { font-family: 'Caveat', cursive; font-size: 22px; color: var(--muted); margin-bottom: 28px; }

    /* MUSIC */
    .music {
      display: grid; grid-template-columns: 1.05fr 1fr; gap: 36px; align-items: start;
    }
    @media (max-width: 900px) { .music { grid-template-columns: 1fr; } }

    .tracklist {
      background: #fff; border: 1px solid rgba(0,0,0,0.06); border-radius: 16px; padding: 18px 18px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.06);
    }
    .track { display: grid; grid-template-columns: 22px 1fr auto; gap: 12px; align-items: center; padding: 10px 8px; border-radius: 10px; }
    .track:hover { background: rgba(0,0,0,0.035); }
    .track .dot { width: 10px; height: 10px; background: var(--accent); border-radius: 50%; }
    .track .name { font-weight: 500; }
    .track .mood { color: var(--muted); font-size: 12px; }

    .cover {
      position: relative; border-radius: 18px; overflow: hidden; border: 1px solid rgba(0,0,0,0.06);
      box-shadow: 0 24px 60px rgba(0,0,0,0.12);
    }
    .cover img { display: block; width: 100%; height: auto; }
    .cover figcaption {
      position: absolute; inset: auto 0 0 0; padding: 14px 16px; color: #fff; backdrop-filter: blur(2px);
      background: linear-gradient( to top, rgba(0,0,0,0.55), transparent ); font-size: 14px;
    }

    .player {
      margin-top: 16px; border: 1px solid rgba(0,0,0,0.08); border-radius: 14px; overflow: hidden;
      background: #fff; box-shadow: 0 10px 26px rgba(0,0,0,0.06);
    }
    .player iframe { display: block; width: 100%; height: 166px; border: 0; }

    /* PHOTOS */
    .grid {
      display: grid; gap: 14px; grid-template-columns: repeat(12, 1fr);
    }
    .grid .ph { position: relative; overflow: hidden; border-radius: 14px; background: #e9e7e2; border: 1px solid rgba(0,0,0,0.06); box-shadow: 0 12px 26px var(--photo-edge); }
    .grid .ph img { width: 100%; height: 100%; object-fit: cover; transition: transform 600ms ease; display: block; }
    .grid .ph:hover img { transform: scale(1.06); }
    .grid .ph figcaption { position: absolute; inset: auto 10px 10px 10px; background: rgba(0,0,0,0.45); color: #fff; padding: 8px 10px; border-radius: 8px; opacity: 0; transition: 220ms ease; font-family: 'Caveat', cursive; }
    .grid .ph:hover figcaption { opacity: 1; }

    /* Masonry-like spans */
    .c4 { grid-column: span 4; height: 240px; }
    .c6 { grid-column: span 6; height: 280px; }
    .c8 { grid-column: span 8; height: 320px; }
    .tall { height: 380px; }
    @media (max-width: 900px) {
      .grid { grid-template-columns: repeat(6, 1fr); }
      .c8 { grid-column: span 6; }
      .c6 { grid-column: span 6; }
      .c4 { grid-column: span 3; }
      .tall { height: 300px; }
    }
    @media (max-width: 560px) {
      .grid { grid-template-columns: repeat(2, 1fr); }
      .c8, .c6, .c4 { grid-column: span 2; }
      .tall { height: 220px; }
    }

    /* ABOUT */
    .about {
      display: grid; grid-template-columns: 0.9fr 1.1fr; gap: 36px; align-items: start;
    }
    @media (max-width: 900px) { .about { grid-template-columns: 1fr; } }

    .portrait { aspect-ratio: 4/5; border-radius: 18px; overflow: hidden; border: 1px solid rgba(0,0,0,0.06); box-shadow: 0 24px 60px rgba(0,0,0,0.12); background: #e8e6e0; }
    .portrait img { width: 100%; height: 100%; object-fit: cover; display: block; }

    .bio { background: #fff; border: 1px solid rgba(0,0,0,0.06); border-radius: 16px; padding: 20px; box-shadow: 0 12px 30px rgba(0,0,0,0.06); }
    .bio p { margin-top: 0; }
    .timeline { list-style: none; padding: 0; margin: 12px 0 0; color: var(--muted); }
    .timeline li { padding: 6px 0; }

    .signature { font-family: 'Caveat', cursive; font-size: 28px; margin-top: 12px; color: var(--ink); }

    /* Footer */
    footer { padding: 46px 0 60px; text-align: center; color: var(--muted); font-size: 14px; }

    /* Custom cursor follower */
    .cursor-dot {
      position: fixed; top: 0; left: 0; width: 10px; height: 10px; border-radius: 50%;
      background: var(--accent); opacity: 0.9; transform: translate(-50%, -50%);
      pointer-events: none; z-index: 9999; transition: width 160ms ease, height 160ms ease, opacity 160ms ease;
      mix-blend-mode: multiply;
    }
    a:hover ~ .cursor-dot, .track:hover ~ .cursor-dot { width: 14px; height: 14px; opacity: 1; }
  </style>
</head>
<body class="at-hero">
  <nav class="nav" aria-label="Primary">
    <a href="#music">Music</a>
    <a href="#photos">Photos</a>
    <a href="#about">About</a>
  </nav>

  <header id="home" class="hero">
    <div class="container">
      <h1 class="title">Your Name</h1>
      <div class="subtitle">music & film photography</div>
      <div class="hand">stories in sound + light</div>
    </div>
    <div class="scrollhint">scroll ↓</div>
  </header>

  <main>
    <section id="music">
      <div class="container">
        <h2 class="section-title">Music</h2>
        <div class="section-note">slow mornings, late-night rooftops</div>
        <div class="music">
          <div class="tracklist">
            <div class="track"><div class="dot"></div><div><div class="name">Sunday Rain on Rooftops</div><div class="mood">ambient / mellow</div></div><div>3:18</div></div>
            <div class="track"><div class="dot"></div><div><div class="name">Neon Reflections</div><div class="mood">lofi / night drive</div></div><div>2:47</div></div>
            <div class="track"><div class="dot"></div><div><div class="name">Sidewalk Constellations</div><div class="mood">minimal / warm</div></div><div>4:01</div></div>
            <div class="track"><div class="dot"></div><div><div class="name">Quiet Rooftops</div><div class="mood">cinematic / soft</div></div><div>3:36</div></div>
          </div>

          <figure class="cover">
            <img src="https://images.unsplash.com/photo-1519681393784-d120267933ba?q=80&w=1600&auto=format&fit=crop" alt="Album cover mood" />
            <figcaption>"City hum in slow motion" — EP cover</figcaption>
            <div class="player">
              <!-- Replace with your Bandcamp or SoundCloud embed -->
              <iframe title="SoundCloud Player" scrolling="no" frameborder="no"
                src="https://w.soundcloud.com/player/?url=https%3A//api.soundcloud.com/tracks/293&color=%23a0a0a0&inverse=false&auto_play=false&show_user=true"></iframe>
            </div>
          </figure>
        </div>
      </div>
    </section>

    <section id="photos">
      <div class="container">
        <h2 class="section-title">Photos</h2>
        <div class="section-note">35mm streets & landscapes</div>

        <div class="grid">
          <figure class="ph c8 tall"><img src="https://images.unsplash.com/photo-1532298229144-0ec0c57515c7?q=80&w=1600&auto=format&fit=crop" alt=""/><figcaption>corner of 8th &amp; forgotten</figcaption></figure>
          <figure class="ph c4"><img src="https://images.unsplash.com/photo-1523741543316-beb7fc7023d8?q=80&w=1200&auto=format&fit=crop" alt=""/><figcaption>window light, 4pm</figcaption></figure>
          <figure class="ph c6"><img src="https://images.unsplash.com/photo-1469474968028-56623f02e42e?q=80&w=1400&auto=format&fit=crop" alt=""/><figcaption>desert line</figcaption></figure>
          <figure class="ph c6 tall"><img src="https://images.unsplash.com/photo-1433838552652-f9a46b332c40?q=80&w=1400&auto=format&fit=crop" alt=""/><figcaption>neon rain</figcaption></figure>
          <figure class="ph c4"><img src="https://images.unsplash.com/photo-1482192505345-5655af888cc4?q=80&w=1200&auto=format&fit=crop" alt=""/><figcaption>rooftop hush</figcaption></figure>
          <figure class="ph c8"><img src="https://images.unsplash.com/photo-1472214103451-9374bd1c798e?q=80&w=1600&auto=format&fit=crop" alt=""/><figcaption>coastline haze</figcaption></figure>
        </div>
      </div>
    </section>

    <section id="about">
      <div class="container">
        <h2 class="section-title">About</h2>
        <div class="section-note">hi, i'm [your name] — i make songs & shoot film</div>
        <div class="about">
          <div class="portrait"><img src="https://images.unsplash.com/photo-1544005313-94ddf0286df2?q=80&w=1200&auto=format&fit=crop" alt="Portrait"/></div>
          <div class="bio">
            <p>I shoot the moments most people walk past—cracked sidewalks, neon reflections, quiet rooftops. My music leans ambient and lofi; the photos are mostly 35mm. Together they’re a little zine of the places I call home.</p>
            <ul class="timeline">
              <li>📷 2019 — first thrifted SLR (Canon AE-1)</li>
              <li>🎵 2022 — released first EP, <em>Sidewalk Constellations</em></li>
              <li>🗺️ 2024 — desert road trip series</li>
              <li>🎧 2025 — scoring a short film</li>
            </ul>
            <div class="signature">— your name</div>
          </div>
        </div>
      </div>
    </section>
  </main>

  <footer>
    <div class="container">© <span id="year"></span> Your Name — all images & audio by me</div>
  </footer>

  <div class="cursor-dot" aria-hidden="true"></div>

  <script>
    // Year
    document.getElementById('year').textContent = new Date().getFullYear();

    // Body tint by section in view
    const sections = [
      { id: 'home', cls: 'at-hero' },
      { id: 'music', cls: 'at-music' },
      { id: 'photos', cls: 'at-photos' },
      { id: 'about', cls: 'at-about' },
    ];
    const io = new IntersectionObserver(entries => {
      entries.forEach(e => {
        if (e.isIntersecting) {
          document.body.className = sections.find(s => s.id === e.target.id)?.cls || '';
        }
      });
    }, { threshold: 0.5 });
    sections.forEach(s => {
      const el = document.getElementById(s.id);
      if (el) io.observe(el);
    });

    // Simple cursor follower
    const dot = document.querySelector('.cursor-dot');
    let x = window.innerWidth / 2, y = window.innerHeight / 2;
    let tx = x, ty = y;
    const speed = 0.18;
    function animate(){
      x += (tx - x) * speed;
      y += (ty - y) * speed;
      dot.style.transform = `translate(${x}px, ${y}px)`;
      requestAnimationFrame(animate);
    }
    window.addEventListener('mousemove', (e)=>{ tx = e.clientX; ty = e.clientY; });
    animate();
  </script>
</body>
</html>
